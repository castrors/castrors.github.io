name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install Dart 🎯
        uses: dart-lang/setup-dart@v1

      - name: Install dependencies 📦
        run: dart pub get

      - name: Install Jaspr CLI 🛠️
        run: dart pub global activate jaspr_cli

      - name: Create Firebase Config File 🔑
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASECONFIG }}
        run: echo "$FIREBASE_CONFIG" > firebase_config.json

      - name: Clear build daemon locks 🧹
        run: |
          rm -rf $HOME/.dart_tool/build/daemon || true
          rm -rf .dart_tool/build || true

      - name: Build 🏗️
        run: |
          # Pre-start build daemon without managed options to avoid dynamic define mismatches
          dart run build_runner daemon --release --verbose --delete-conflicting-outputs &
          sleep 50
          # Run jaspr build with --no-managed-build-options so requested options match running daemon 100%
          $HOME/.pub-cache/bin/jaspr build --no-managed-build-options

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build/jaspr
          branch: gh-pages
